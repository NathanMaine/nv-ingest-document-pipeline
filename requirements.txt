# Core - NVIDIA nv-ingest (only needed when actually extracting; tests run without it)
nv-ingest>=26.1.2
nv-ingest-api>=26.1.2
nv-ingest-client>=26.1.2

# Baseline comparison for benchmark.py
PyPDF2>=3.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
